# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler
/vendor/
.bundle/

# macOS
.DS_Store

# Local-only source photos (full-res originals — crop/optimize in place, never committed).
# The web-optimized derivatives live in assets/images/portfolio/ and ARE published.
assets/images/wedding_photos/

# Internal SEO briefing: local-only, never published
seo-report/

# CI scratch for responsive image variants (see scripts/make-responsive-variants.sh)
.rsp-cache/

# Proposal system archive: removed from the public repo 2026-07-11, kept local-only
proposal-archive/

# Local design prototypes for owner review, never published
*-preview.html
